info:
  name: Consultar cobranca
  type: http
  seq: 2

http:
  method: GET
  url: "{{pagamentoUrl}}/cobrancas/{{assinaturaId}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setVar("paymentId", res.body.paymentId);

settings:
  encodeUrl: true
