info:
  name: Listar falhas da outbox (pagamento)
  type: http
  seq: 4

http:
  method: GET
  url: "{{pagamentoUrl}}/outbox/falhas?page=0&size=20"
  headers:
    - name: Authorization
      value: "Bearer {{tokenAdmin}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        expect(res.getStatus()).to.equal(200);
        expect(res.body.items, "lista de falhas").to.be.an('array');

settings:
  encodeUrl: true
